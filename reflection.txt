Name : Rachel Gillespie

Student Number: 20118715

Chronology of your implementation (what you did first, etc...):

1) I started on the 'models' (Inheritance Hierarchy) part of the assignment as suggested in the 'Plan of Action/Attack'.
    - I declared the instance fields with private access and generated the constructor and getter/setter methods for Pet, Dog and Cat. I also generated the toString() method for Pet, Cat and Dog, and the equals() method for Dog.
    - I moved the isNeutered method from the Dog subclass to the Pet superclass as this can be used for every pet to different pets.
    - The Pet, Cat and Dog classes also have a calculateWeeklyFee() method, the subclasses of Pet override the method in the superclass.
    - Pet has check in/check out methods, they return nothing, a number indicating the day of the week (between 0 and 4 inclusive) is passed to the method.
    - Pet also has numOfDaysInKennel that iterates through the daysAttending boolean Array and returns the number of days that were assigned true.

2) I then moved onto the 'controllers' (DayCare Class):

    - I began by declaring the instance fields with private access and generated the constructor and getter/setter methods for the class.
    - I then moved on to writing headers and basic implementation for the methods (as per UML) to get the code compiled.
    - I started with basic CRUD methods, update methods
    - I used List<Pet> pets instead of ArrayList<Pet> because it provides flexibility to change the underlying implementation without breaking existing code, following the principle of programming to an interface rather than a concrete implementation.
    - I Changed updatePet to return a boolean instead of a Pet object, as it was easier to check if one was updated successfully, returning true/false rather than a Pet object.
    - Instead of writing a method for isValidPetIndex() in the DayCare class, I used a isValidIndex() method from the Utilities Class.

3) I then moved onto the Driver and UX:

    - I wrote the driver as before, using the usual console-based menu system.

4) DX (Overall Style):

    - I commented my code along the way where I thought it was necessary .
    - I used standard naming, indentation and I tried to use DRY Code where possible.
    - I wrote Javadoc for the DayCare class.

5) Extras (not in Spec):

    - Added Rabbit and RabbitBreedUtility Classes
    - Added DayCare setting menu
    - Added a clear menu system

Main difficulties you came across in your development of solution and how you solved them:

    - I ran out of time to complete this project to the standards I would have liked, I tried to make sure that what I had completed is without bugs and errors and with everything working smoothly.

Any bugs remaining in the solution or unfinished elements of spec:

    - ID doesn't update when updating a pet.

Main learnings from your engagement with assignment:

    - Programming is mostly about fixing bugs and errors.
    - You always need more time than you think.

Answer the following questions:

     1. Pat Purrs mentioned a problem their friend had with their pet system. What was the issue with that system, and how is your design intended to avoid the same problem?
        The issue with that system was that it wasn't built very well, it lacked things a good system needs.

     2. Why does Pat want the Driver class to be simple and user-friendly, and how does this relate to the plan to eventually develop a mobile app?
        Pat wants the Driver class to be simple and user-friendly as this is the class that the user interacts with. Keeping code organised and well-structured especially the driver class, will make it easier to move to mobile.

     3. Pat talked about future expansion of the daycare. What kinds of growth does the system need to support, and how should your code design accommodate this?
        The system needs to support making adding new animal types easy and clean, by avoiding all kinds of hard coded limits and by being flexible for capacity growth.

     4. Besides cats and dogs, what additional animal does Pat plan to include next, and what does this imply for how you structure your inheritance hierarchy?
        Pat plans to include Rabbits, Ferrets and Alpacas, this implies that the inheritance hierarchy should be structured to allow for these animals to be able to inherit from the superclass of Pet.

Mandatory : Please list any references used in your development/implementation of your submission.:

    - https://stackoverflow.com/questions/147468/why-should-the-interface-for-a-java-class-be-preferred
    - https://stackoverflow.com/questions/9852831/polymorphism-why-use-list-list-new-arraylist-instead-of-arraylist-list-n

Please consider the following statements and choose one (delete the inappropriate one):

    - This is my work apart from the specific references noted above (and any code from class notes). I understand the code and can describe any parts of the solution if needs be;


